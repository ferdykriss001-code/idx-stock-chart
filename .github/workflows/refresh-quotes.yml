name: V3 Full IDX - Refresh 5-minute quotes (manual only)

# Automatic full-market 5-minute refresh is intentionally disabled.
# Use "Manual - Update one IDX ticker from Yahoo" for the ticker you need.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Automatic refresh for all IDX tickers is disabled.'
          echo 'Run: Manual - Update one IDX ticker from Yahoo.'
